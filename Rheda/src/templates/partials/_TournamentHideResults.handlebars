<h3>{{_t 'Скрыть таблицу с результатами игроков'}}</h3>
<p>{{_t 'Это может быть полезно для сохранения интриги на последних ханчанах турнира.'}}</p>

{{#form action='/tourn/toggleHideResults/' method='post'}}
    <input type="submit"
           class="btn btn-large btn-{{#hideResults}}success{{/hideResults}}{{^hideResults}}warning{{/hideResults}} actionButton"
           value="{{#hideResults}}{{_t 'Данные скрыты. Показать'}}{{/hideResults}}
               {{^hideResults}}{{_t 'Данные доступны всем. Скрыть'}}{{/hideResults}}"
    />
{{/form}}