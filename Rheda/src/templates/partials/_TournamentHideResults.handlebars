<div class="row">
    <div class="col-12">
        <h3>{{_t 'Hide/show rating table'}}</h3>
    </div>
</div>
<div class="row">
    <div class="col-12">
        {{_t 'Hiding rating table for all players may be useful to keep everybody intrigued at the end of tournament'}}
    </div>
</div>
<br/>
<div class="row">
    <div class="col-12 form-group">
        {{#form action='/tourn/toggleHideResults/' method='post'}}
            <input type="submit"
                   class="btn btn-{{#hideResults}}success{{/hideResults}}{{^hideResults}}warning{{/hideResults}} form-control"
                   value="{{#hideResults}}{{_t 'Rating table is hidden. Show'}}{{/hideResults}}{{^hideResults}}{{_t 'Rating table is visible. Hide'}}{{/hideResults}}"
            />
        {{/form}}
    </div>
</div>
<hr />
