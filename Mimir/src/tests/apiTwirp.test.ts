import { MimirTest } from 'src/services/MimirTest';

describe('Mimir Twirp API', () => {
  const mimirClient = new MimirTest();
  beforeAll(() => {
    // prepare test data in db
  });

  test('should return a list of users', async () => {});
});
