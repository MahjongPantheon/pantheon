export enum PlayerPointsMode {
    IDLE,
    POSITIVE,
    NEGATIVE,
    ACTIVE,
}

export enum PlayerButtonMode {
    IDLE,
    DISABLE,
    PRESSED,
}

export enum PlayerMode {
    TOP,
    RIGHT,
    BOTTOM,
    LEFT,
}
