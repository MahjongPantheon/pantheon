- All items in client should obey the server state
- There should be some incremental counter of actions between states. If counter increases more than in one,
  no intermediate animations should be played, client just should display the server state immediately.
